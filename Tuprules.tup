.gitignore

CC = gcc
CXX = g++
LD = $(CXX)

CPPFLAGS += -O0
CPPFLAGS += -g
CPPFLAGS += -std=c++11
CPPFLAGS += -Wall -Wextra

LDFLAGS =

!cxx = |> ^ CXX %f^ $(CXX) $(CPPFLAGS) -c %f -o %o |> %B.o
!linker = |> ^ LD %o^ $(LD) $(LDFLAGS) %f -o %o |>
